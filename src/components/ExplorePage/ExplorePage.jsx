const ExplorePage = ({ campaigns }) => {
    console.log(campaigns)

    // Search functions

    // Filter functions


    return (
        <>
            <ul>
            {campaigns.map((campaign) => (
                <li></li>
            ))}
            </ul>
        </>
    )
}

export default ExplorePage;